temperatura = 31.5
umbral = 30
alerta = temperatura > umbral
print(f"Temperatura: {temperatura} C")
print(f"Umbral: {umbral} C")
print(f"Alerta activa: {alerta}")

# Practica: baja temperatura a 28.
# Error comun: confundir > con >=.
